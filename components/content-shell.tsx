import type { ReactNode } from "react";
import { ArrowUpRight, ArrowRight } from "lucide-react";
import { serviceContent } from "@/lib/service-content";
export function ContentShell({children}:{children:ReactNode}) {
 return <><div className="preview-bar"><span>PRIMER VISTAZO</span> Proyecto en preparación · Sin reservas ni cobros reales</div><a className="skip-link" href="#contenido">Saltar al contenido</a><header className="header"><div className="shell nav"><a href="/" className="brand" aria-label="Chombly, inicio">chombly.</a><nav className="content-nav" aria-label="Navegación principal"><a href="/#servicios">Servicios</a><a href="/sobre-chomby">Sobre Chombly</a><a href="/profesionales">Profesionales</a></nav><a className="button small" href="/agendar/veterinario-online-colombia">Consulta online <ArrowRight size={16}/></a></div></header><main id="contenido">{children}</main><footer><div className="shell footer-main"><a className="brand" href="/">chombly.</a><p>Cuidado animal, más cerca.</p><span>Colombia · Lanzamiento en preparación</span></div><nav className="shell footer-services" aria-label="Servicios y páginas de Chombly">{serviceContent.map(s=><a href={`/servicios/${s.slug}`} key={s.slug}>{s.name}</a>)}<a href="/sobre-chomby">Sobre Chombly</a><a href="/profesionales">Para profesionales</a></nav><div className="shell footer-bottom"><span>© 2026 Chombly</span><span>Precios, profesionales y cobertura por confirmar.</span></div></footer></>;
}
export function RelatedServices({exclude}:{exclude?:string}) {return <section className="shell related-services"><p className="eyebrow">SIGUE EXPLORANDO</p><h2>Más cuidados para tu animal</h2><div>{serviceContent.filter(s=>s.slug!==exclude).map(s=><a key={s.slug} href={`/servicios/${s.slug}`}>{s.name}<ArrowUpRight size={17}/></a>)}</div></section>}
export function Questions({items}:{items:readonly (readonly string[])[]}) {return <section className="detail-faq" id="preguntas"><h2>Preguntas frecuentes</h2><div className="faq-list">{items.map(([q,a])=><details key={q}><summary>{q}<span>+</span></summary><p>{a}</p></details>)}</div></section>}
